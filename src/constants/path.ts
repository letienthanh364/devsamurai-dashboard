// Define the main paths
const mainPaths = {
  home: "/",
  login: "/auth/login",
  signup: "/auth/signup",
};

export default mainPaths;

export const dashboardPaths = {
  home: "/dashboard/home",
};
